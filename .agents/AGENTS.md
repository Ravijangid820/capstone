# Agent Rules

- **Always Stage, Commit, and Push to Git:** Whenever a code change, documentation update, or figure generation task is completed and verified, you must automatically stage all modifications (`git add`), commit them with a descriptive commit message (`git commit`), and push (`git push`) to the remote repository without requiring a reminder from the user.
